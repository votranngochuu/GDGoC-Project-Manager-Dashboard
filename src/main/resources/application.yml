server:
  port: 8080
  servlet:
    context-path: /gdgoc_dashboard

# Frontend: served from project folder "frontend/". BACKEND_URL used by env.js.
app:
  frontend:
    path: frontend
    backend-url: /gdgoc_dashboard/api
    # Firebase client config (copy từ Firebase Console → Project settings → Your apps)
    firebase:
      api-key: "AIzaSyD692wzVcc35Lo5fhrD8oIvGFGMacChxzQ"
      auth-domain: "gdgoc-performance-dashboard.firebaseapp.com"
      project-id: "gdgoc-performance-dashboard"
      storage-bucket: "gdgoc-performance-dashboard.firebasestorage.app"
      messaging-sender-id: "988792928825"
      app-id: "1:988792928825:web:57da898b54f1448eb02be8"
      measurement-id: "G-V714JC0VQE"

spring:
  application:
    name: gdgoc-dashboard

  datasource:
    url: jdbc:postgresql://localhost:5432/gdgoc_dashboard
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

firebase:
  service-account-path: firebase-service-account.json
